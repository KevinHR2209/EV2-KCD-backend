name: CI/CD - Backend Despachos

on:
  push:
    branches:
      - deploy

jobs:
  build-and-deploy:
    name: Build, Push y Deploy - Despachos
    runs-on: ubuntu-latest

    steps:
      - name: Checkout codigo
        uses: actions/checkout@v4

      - name: Login a Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build y Push imagen ms-despacho
        uses: docker/build-push-action@v5
        with:
          context: ./back-Despachos_SpringBoot
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/ev2-kcd-ms-despacho:latest

      - name: Deploy en EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/ev2-kcd-ms-despacho:latest
            docker stop ms-despacho || true
            docker rm ms-despacho || true
            docker network create backend-net || true
            docker run -d \
              --name ms-despacho \
              --network backend-net \
              --restart unless-stopped \
              -p 8081:8081 \
              -e DB_ENDPOINT=mysqldb \
              -e DB_PORT=3306 \
              -e DB_NAME=despacho_db \
              -e DB_USERNAME=${{ secrets.DB_USERNAME }} \
              -e DB_PASSWORD=${{ secrets.DB_PASSWORD }} \
              ${{ secrets.DOCKERHUB_USERNAME }}/ev2-kcd-ms-despacho:latest
